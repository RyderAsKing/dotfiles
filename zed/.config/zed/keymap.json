[
  {
    "context": "Workspace && !menu",
    "bindings": {
      "ctrl-h": "workspace::ActivatePaneLeft",
      "ctrl-j": "workspace::ActivatePaneDown",
      "ctrl-k": "workspace::ActivatePaneUp",
      "ctrl-l": "workspace::ActivatePaneRight"
    }
  },

  {
    "context": "Editor && VimControl && !VimWaiting && !menu",
    "bindings": {
      "space f": "file_finder::Toggle",
      "space o": "tab_switcher::Toggle",
      "space e": "project_panel::Toggle",
      "space p": "outline::Toggle",
      "space g": "git_panel::Toggle",
      "space a": "agent::Toggle",
      "space t": "terminal_panel::Toggle",
      "space v": "editor::GoToDefinitionSplit",

      // LSP keybinds
      "space l a": "editor::ToggleCodeActions",
      "space l r": "editor::Rename",
      "space l f": "editor::Format",
      "space l d": "diagnostics::Deploy",

      // Diagnostics/References
      "g d": "editor::GoToDefinition",
      "g r": "editor::FindAllReferences",
      "shift-k": "editor::Hover",

      // Workspace Management
      "ctrl-w z": "workspace::ToggleZoom",
      "ctrl-w t": "terminal_panel::Toggle",

      "ctrl-h": "workspace::ActivatePaneLeft",
      "ctrl-j": "workspace::ActivatePaneDown",
      "ctrl-k": "workspace::ActivatePaneUp",
      "ctrl-l": "workspace::ActivatePaneRight",
    },
  },

  {
    "context": "Editor && vim_mode == visual && !VimWaiting && !VimObject",
    "bindings": {
      "shift-j": "editor::MoveLineDown",
      "shift-k": "editor::MoveLineUp",
    },
  },

  {
    "context": "Terminal",
    "bindings": {
      "ctrl-s": null,
      "ctrl-w t": "terminal_panel::Toggle",

      "ctrl-h": "workspace::ActivatePaneLeft",
      "ctrl-j": "workspace::ActivatePaneDown",
      "ctrl-k": "workspace::ActivatePaneUp",
      "ctrl-l": "workspace::ActivatePaneRight",
    },
  },
  {
    "bindings": {
      "ctrl-shift-k ctrl-shift-o": "workspace::Open"
    }
  },
  {
    "unbind": {
      "ctrl-k ctrl-o": "workspace::Open"
    }
  },
  {
    "context": "Workspace",
    "unbind": {
      "ctrl-alt-j": "multi_workspace::ToggleWorkspaceSidebar"
    }
  },
  {
    "bindings": {
      "ctrl-alt-j": "multi_workspace::NextProject"
    }
  },
  {
    "context": "ThreadsSidebar && !Editor",
    "unbind": {
      "] p": "multi_workspace::NextProject"
    }
  }
]
